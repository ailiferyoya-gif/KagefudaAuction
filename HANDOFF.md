# 影札オークション 引継ぎ

## 仮想デスクトップ（2026-07-10追加）

- 入口: `desktop.html`。Windows 11風の全画面端末から既存サイト、LINE、夜間引継ぎ、ごみ箱、観測記録へ移動できる。
- 実装: `desktop.css` / `desktop.js`。ウィンドウの前面化・ドラッグ・最小化・最大化、スタートメニュー、通知、時計、モバイル全画面表示を実装。
- LINE: 複数トーク、キーワード応答、ヒント段階解放、架空アーカイブ内線キーパッド、通話履歴を実装。外部通信とマイク取得は行わない。
- 通話: 特殊な12桁内線で呼出音が4回鳴った後、`assets/call-031711310417.wav` を再生。正解後はLINE・通知・時計が連動する。
- 生成素材: `assets/desktop-wallpaper.png`（青い抽象リボンの端末壁紙）をChatGPTで生成。スプライトシート不使用。
- 端末内ファイル: 夜間引継ぎ、音声検査要約、運用メモ、ごみ箱の削除済み通話記録をメモ帳で閲覧できる。
- 直接起動アプリ: 音声レコーダー、カレンダー、写真、地図、ダウンロード、監視カメラ、付箋、設定をデスクトップに個別配置。各アプリは独立ウィンドウとしてドラッグ・最小化・最大化・閉じる操作ができる。
- 読み込み方式: 各アプリのiframeは初回起動時だけ `tools.html?embed=1&app=<id>` を読み込む。ライブラリUIを隠し、指定アプリだけを表示するため、初期デスクトップを重くしない。
- 保存: `kagefuda-desktop-v1`（端末状態）と既存の観測者プロトコル用キーを同一オリジンの `localStorage` で管理。
- 安全境界: 110・119を含む実電話網には接続しない。全演出はHTML/CSS/JavaScriptと同梱音声だけで完結。

## Google風ローカル検索（2026-07-10追加）

- 実装: `search.html` / `search.css` / `search.js`。仮想EdgeのホームおよびデスクトップのGoogleアイコンから開く。
- 操作: 検索候補、キーボード選択、最近の検索、結果タブ、該当なし状態、I'm Feeling Lucky相当の導線を実装。
- 索引: 影札トップ、開催中ロット、LOT 0417、来歴、Private Sale、観測記録など同一サイト内のページだけを検索する。
- ARG連携: 「早瀬」「11月31日」「03:17」「第七码頭」などで通常結果に混ざる不穏な記録が変化する。完全な電話番号は検索だけでは露出しない。
- 安全境界: 外部Google、Gmail、画像検索、音声検索、カメラには接続しない。マイク・カメラ権限も要求しない。
- 保存: 最近の検索のみ `kagefuda-search-v1` として同一ブラウザ内に保存する。

## ローカルMail（2026-07-10追加）

- 入口: `desktop.html` のデスクトップアイコン、スタートメニュー、タスクバー、通知から `mailWindow` を開く。
- 実装: `desktop.html` / `desktop.css` / `desktop.js`。3ペインの受信トレイ、本文表示、メール検索、未読、スター、アーカイブ、ごみ箱、下書き、送信済みを実装。
- ARG連携: 宛先のない復元メール、早瀬の未送信返信、存在しない識別子への配信不能通知、保安ログを配置。電話番号の答えを単独では断定しない。
- 保存と安全境界: 状態は既存の `kagefuda-desktop-v1` 内の `mail` にのみ保存。送信操作は端末内の「送信済み」へ保存するだけで、外部メール、認証、添付、ネットワーク通信は発生しない。
- モバイル: 375px幅ではメール一覧と本文を1ペインで切り替え、本文から一覧へ戻る操作を提供する。

## 謎解きアプリライブラリ（2026-07-10追加）

- 入口: 全アプリ一覧は `tools.html`。`desktop.html` の「謎解きツール」アイコン、スタートメニュー、Google風ローカル検索から開く。8アプリはデスクトップの個別アイコンからも直接起動する。URLは `https://ailiferyoya-gif.github.io/KagefudaAuction/tools.html`。
- 実装: `tools.html` / `tools.css` / `tools.js`。アプリを別ページに分離し、事件データは主に `tools.js` 冒頭の配列・辞書で差し替え可能にした。`embed=1&app=<id>` でナビゲーションを除いた単体アプリ表示になる。
- 収録アプリ: 音声レコーダー（再生位置で文字起こしが段階表示）、カレンダー（存在しない日付）、写真（画像選択・拡大・メタデータ）、地図（地点選択）、ダウンロード（ローカル復元状態）、監視カメラ（フレーム移動・欠損復元）、付箋（編集・保存）、設定（演出密度・音の切替）。
- 保存: `kfa-puzzle-tools-v1` にアクティブ画面、再生位置、復元状態、付箋、設定だけを保存。リセット操作は確認後にこのキーの内容を初期状態へ戻す。
- 生成素材: `assets/pier-7-night.png` はChatGPT生成の第七码頭夜景。写真・監視カメラで利用。既存のChatGPT生成素材 `lot-0417-recorder.png` と `lot-0000-reliquary.png` も写真アプリで使用。スプライトシート不使用。
- 安全境界: 外部地図、カメラ、マイク、クラウド復元、ファイル送受信は使用しない。音声レコーダーの信号音もブラウザ内で短く生成するだけ。

- 生成済み画像: `assets/lot-0417-recorder.png`（架空の水損カセットレコーダー）
- 実装: UI/UX Pro MaxのSwiss Modernism 2.0、Editorial Grid、Bento Grid指針で再調整。`private-sale.html`を追加した7ページ構成。
- UI強化: ホーム検索を主CTA化、マーケット指標レール、非対称Private Sale、カタログ表示切替、モバイル4項目ドックとボトムシート、短いページ遷移を追加。
- 共通基盤: `styles.css` と `app.js`。検索モーダル、カタログ絞り込み、ローカル保存ウォッチ、カウントダウン、入札デモ、スクロール表示を実装。
- 画像: 既存3点に加え、`hero-vault.png`、`lot-0711-film.png`、`lot-0731-bell.png`、`lot-0804-box.png` をChatGPTで生成して使用。スプライトシート不使用。
- 謎解きの本線: 「三つの記録が止まった時刻」「存在しない日付」「第七码頭の録音機」を4桁ずつ連結し、架空内線へ発信する。
- 次にやること: 必要ならLOT 0529以降の個別詳細ページ、LOT 0000受諾後にデスクトップをさらに変化させる第2章、アプリ間イベントを組み替える事件別シナリオ設定ファイルを追加する。

## 公開先

- GitHub: https://github.com/ailiferyoya-gif/KagefudaAuction
- GitHub Pages: https://ailiferyoya-gif.github.io/KagefudaAuction/
- 仮想デスクトップ: https://ailiferyoya-gif.github.io/KagefudaAuction/desktop.html
- Pages設定: `main` ブランチ / `/ (root)` / `.nojekyll`

## 観測者プロトコル

- `observer.js` が閲覧ページ、検索語、ウォッチ状態を端末内の `localStorage` にのみ記録。
- `observer.html` で個別ID、行動ログ、証拠関係図、段階解放を表示。
- 条件達成と照合語の復元で `lot-0000.html` が解放される。
- 隠しロット画像は `assets/lot-0000-reliquary.png`。ChatGPT生成、スプライトシート不使用。
